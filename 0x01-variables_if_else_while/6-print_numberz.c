#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entering digits
 *
 * Return: just 0 (succsee)
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	putcahr(i);
	putchar("\n");

	return (0);
}
