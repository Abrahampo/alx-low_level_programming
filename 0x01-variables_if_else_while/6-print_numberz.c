#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entering digits
 *
 * Return: just 0 (succsee)
 */
int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
	putcahr(d);
	putchar("\n");

	return (0);
}
