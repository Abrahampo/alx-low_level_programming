#include "main.h"

/**
 * mul - multiplies two integers
 * @X: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of mulplication betwee x param and y param
 */
int mul(int x, int y)

{
	return (x * Y);
}
